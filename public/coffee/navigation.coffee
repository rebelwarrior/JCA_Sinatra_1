#%a.dropdown-toggle{:'data-toggle' => "dropdown", :href =>"#"}

$('.dropdown-toggle').dropdown()
$('.nav-tabs').button()
$('table').addClass('table table-bordered') #needs to go after page loads
$('tr').addClass('info')
#Put inside file since it's just one line!
