// Generated by CoffeeScript 1.6.2
(function() {
  var alberto, getLocation, showPosition, testGeolocation;

  showPosition = function(position) {
    return [position.coords.latitude, position.coords.longitude];
  };

  getLocation = function() {
    if (testGeolocation()) {
      return showPosition(navigator.geolocation.getCurrentPosition(showPosition, showError));
    } else {
      return "Geolocation not supported by this browser.";
    }
  };

  testGeolocation = function() {
    if (navigator.geolocation) {
      return true;
    } else {
      return false;
    }
  };

  alberto = true;

}).call(this);
